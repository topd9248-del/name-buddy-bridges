name: Keep Render Alive 🟢

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping buddy-simple
        run: curl -s https://buddy-simple.onrender.com

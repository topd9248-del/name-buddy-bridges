name: Keep Render Alive 🟢

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bridge-simple
        run: curl -s -o /dev/null -w "%{http_code}" https://bridge-simple-test.onrender.com && echo ""
      
      - name: Ping bridge-searchbot
        run: curl -s -o /dev/null -w "%{http_code}" https://bridge-searchbot.onrender.com && echo ""

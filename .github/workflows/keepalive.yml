name: Keep Render Alive 🟢

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bridge-simple
        run: curl -s https://bridge-simple-test.onrender.com
      - name: Ping bridge-searchbot
        run: curl -s https://bridge-searchbot.onrender.com
      - name: Ping bridge-autofilter
        run: curl -s https://bridge-autofilter.onrender.com

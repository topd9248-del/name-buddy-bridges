name: Keep Alive 🟢

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping all services
        run: |
          curl -s https://buddy-movies.onrender.com && echo " movies OK"
          curl -s https://buddy-notify.onrender.com && echo " notify OK"
          curl -s https://buddy-invite.onrender.com && echo " invite OK"
          curl -s https://buddy-delete.onrender.com && echo " delete OK"
          curl -s https://buddy-chatgpt.onrender.com && echo " chatgpt OK"
